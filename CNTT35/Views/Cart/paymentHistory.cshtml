
@{
    ViewBag.Title = "paymentHistory";
    int stt = 0;
    int stt2 = 0;
}
@model List<CNTT35.Data.LichSuMuaHang_Result>
<section class="checkout spad">
    <div class="container">
        <div class="checkout__form">
            <h4>Thông tin đơn hàng</h4>

        </div>
    </div>

    <div class="container">
        <ul class="nav nav-tabs" role="tablist" style="font-weight: 800;">

            <li class="nav-item">
                <a class="nav-link" data-toggle="tab" href="#tabs-1" role="tab" aria-selected="false">Tất cả Đơn hàng</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" data-toggle="tab" href="#tabs-2" role="tab" aria-selected="false">Đơn Hàng mua hàng thành công </a>
            </li>
        </ul>

        <div class="tab-content">

            <div class="tab-pane" id="tabs-1" role="tabpanel">
                <div class="product__details__tab__desc">

                    <div class="container" style="">
                        <div class="row">

                            <table class="table">
                                <thead class="table-light">
                                    <tr>
                                        <th scope="col">STT</th>
                                        <th scope="col">ID đơn hàng</th>
                                        <th scope="col">Tên khách hàng</th>
                                        <th scope="col">Ngày tạo đơn</th>
                                        <th scope="col">Trạng thái đơn hàng</th>
                                        <th scope="col">Tổng tiền</th>
                                    </tr>
                                </thead>
                                @foreach (var item in Model)
                                {
                                    stt = stt + 1;
                                    <tbody>
                                        <tr>
                                            <th scope="row">@stt</th>
                                            <td>@item.IDDH</td>
                                            <td>@item.HOTEN</td>
                                            <td>@item.NGAYLAP.Value.ToString("MM/dd/yyyy")</td>
                                            @if (@item.NGAYGIAOHANG != null)
                                            {
                                                <td><input type="checkbox" checked disabled />Đã Giao</td>

                                            }
                                            else
                                            {
                                                <td><input type="checkbox" disabled />Chưa Giao</td>

                                            }
                                            <td>@string.Format("{0:#,### đ}", @item.THANHTIEN) </td>
                                        </tr>
                                    </tbody>
                                }
                            </table>

                        </div>
                    </div>
                </div>
            </div>
            <div class="tab-pane" id="tabs-2" role="tabpanel">
                <div class="product__details__tab__desc">
                    <div class="container" style="">
                        <div class="row">
                            <table class="table">
                                <thead class="table-light">
                                    <tr>
                                        <th scope="col">STT</th>
                                        <th scope="col">ID đơn hàng</th>
                                        <th scope="col">Tên khách hàng</th>
                                        <th scope="col">Ngày tạo đơn</th>
                                        <th scope="col"> Ngày Vận Chuyển</th>
                                        <th scope="col">Ngày Giao</th>
                                        <th scope="col">Tổng tiền</th>

                                    </tr>
                                </thead>
                                @foreach (var item in Model)
                                {
                                    stt2 = stt2 + 1;
                                    if (item.NGAYGIAOHANG != null)
                                    {
                                        <tbody>
                                            <tr>
                                                <th scope="row">@stt2</th>
                                                <td>@item.IDDH</td>
                                                <td>@item.HOTEN</td>
                                                <td>@item.NGAYLAP.Value.ToString("MM/dd/yyyy")</td>
                                                <td>@item.NGAYVANCHUYEN.Value.ToString("MM/dd/yyyy")</td>
                                                <td>@item.NGAYGIAOHANG.Value.ToString("MM/dd/yyyy")</td>
                                                <td>@string.Format("{0:#,### đ}", @item.THANHTIEN)</td>
                                            </tr>                              
                                       </tbody>
                                    }
                                }
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div>

    </div>

    <div class="col-lg-12 container">
        <div class="shoping__cart__btns">
            <a href="../Product" class="primary-btn cart-btn">Tiếp tục mua sắm</a>
            <a href="../Home" class="primary-btn cart-btn cart-btn-right" style="margin-left: 2rem;">

                <svg xmlns="http://www.w3.org/2000/svg" width="26" height="26" fill="currentColor" class="bi bi-arrow-clockwise" viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M8 3a5 5 0 1 0 4.546 2.914.5.5 0 0 1 .908-.417A6 6 0 1 1 8 2v1z" />
                    <path d="M8 4.466V.534a.25.25 0 0 1 .41-.192l2.36 1.966c.12.1.12.284 0 .384L8.41 4.658A.25.25 0 0 1 8 4.466z" />
                </svg>
                Quay về trang chủ
            </a>
        </div>
    </div>
</section>

